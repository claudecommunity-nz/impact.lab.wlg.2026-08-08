{
  "name": "@wcc-impact/scenario",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Scenario engine for the WCC emergency hack: the scripted southerly-storm timeline plus the stateless replay logic behind the mock weather + social feeds (PLAN \u00a710). Consumed as source by apps/dashboard's /api/scenario/* route handlers.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./timeline.json": "./timeline.json"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "~5.9.3"
  }
}
